<% content_for :title do %>
<%= @user.nickname %>さんのマイページ - ここどこクイズ
<% end %>

<%= render :partial => "header" %>

<h2><%= @user.nickname %>さんのマイページ</h2>

<div style="padding:4px">
<h2>投稿クイズ</h2>
</div>

<div>
投稿クイズ数：<strong><%= @user.quizzes.size %></strong>件
</div>

<table>
<% for quiz in @quizzes %>
  <tr>
    <th><%= link_to quiz.title, quiz %></th>
<% if @user.id == session[:user_id].id %>
    <td><%= link_to '問題一覧を見る', quiz_marks_path(quiz) %></td>
    <td><%= link_to '編集', edit_quiz_path(quiz) %></td>
    <td><%= link_to '削除', quiz, :confirm => 'Are you sure?', :method => :delete %></td>
<% end %>
  </tr>
<% end %>
</table>

<br/>

<div id="footer">
<%= link_to 'クイズを投稿する', new_quiz_path %> | 
<%= link_to 'クイズ一覧をみる', quizzes_path %> |
<%= link_to 'ユーザ情報を変更する', edit_user_path(@user) %> |
<%= link_to 'ユーザ一覧へ', users_path %>
</div>
